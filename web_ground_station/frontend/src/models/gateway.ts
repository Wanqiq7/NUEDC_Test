export interface AckSnapshot { ok:boolean; message:string; task_id:string; mission_loaded:boolean; mission_running:boolean; last_accepted_sequence:number; vision_armed:boolean }
export interface GroundSnapshot { snapshot_seq:number; timestamp_ms:number; active_task_id:string|null; plan:Record<string,unknown>|null; command_link:string; telemetry_link:string; pid_link:string; ack:AckSnapshot|null; mission_loaded:boolean; mission_running:boolean; vision_armed:boolean; current_cell:string|null; visited_count:number; detection_totals:Record<string,number>; recent_detections:Record<string,unknown>[]; target_update:Record<string,unknown>|null; recent_summary:Record<string,unknown>|null; recent_error:Record<string,unknown>|null; recording_error:string }
export interface WebEvent { schema:'nuedc.web.v1'; type:string; seq:number; timestamp_ms:number; task_id:string|null; event:string|null; payload:Record<string,unknown> }
export interface CommandResponse { ok:boolean; message:string; error_code?:string|null; ack?:AckSnapshot|null; plan?:Record<string,unknown> }
